const state = {}

// getters
const getters = {}

const mutations = {}

const actions = {
  action(/*{ commit, state }*/) {
    return new Promise(async (/* resolve, reject */) => {})
  },
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
}
